[project]
name = "uci-252-databricks-retail-lakehouse"
version = "0.1.0"
description = "Retail Lakehouse data engineering project for Databricks"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
